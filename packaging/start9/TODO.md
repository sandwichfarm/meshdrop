# TODO

- Build the package with `make` after StartOS SDK tooling is available.
- Install the package on a StartOS test device.
- Prove a Nostr WebRTC transfer from the StartOS-hosted UI.
- Prove a Pollen transfer after Nostr sign-in loads kind 3 follows and NIP-65 relays from bootstrap relays.
- Decide whether FIPS can be enabled on StartOS after the device FIPS binary and network path are tested.
