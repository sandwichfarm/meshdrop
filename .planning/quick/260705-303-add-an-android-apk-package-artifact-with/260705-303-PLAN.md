---
status: in_progress
quick_id: 260705-303
slug: add-an-android-apk-package-artifact-with
date: 2026-07-05
---

# Quick Task 260705-303: Add an Android APK package artifact with truthful build proof

## Goal

Move Android from native-source-only toward an installable app artifact by adding a reproducible debug APK package build.

## Plan

1. Add an Android APK build mode to the existing mobile package builder.
   - Files: `package.json`, `scripts/build-mobile-package.mjs`, `scripts/android-apk-smoke.mjs`.
   - Action: build the Android native-source project with Gradle, copy `app-debug.apk` into a release tarball, and mark the manifest as a built debug APK without claiming native transfer UAT.
   - Verify: `npm run build:android:apk -- --version 0.0.0-smoke --out-dir /tmp/...` and `npm run test:android-apk`.

2. Wire Android APK artifact proof into CI and release ceremony.
   - Files: `.github/workflows/docker-image.yml`, `.github/workflows/release.yml`, `.github/workflows/release-verify.yml`, workflow tests.
   - Action: add CI smoke for `build:android:apk`, publish the tarball on `v0.*.*`, and require release readback to see it.
   - Verify: `node --test test/ci-workflow.test.js test/release-workflow.test.js`.

3. Update mobile UAT ledger honestly.
   - Files: `docs/uat/mobile.md`, `docs/uat/target-status.md`, `.planning/STATE.md`, quick summary.
   - Action: record that Android debug APK build proof exists, while device install, native WebRTC transfer, file picker/share sheet, Bluetooth, and signed release package remain open.
   - Verify: `node --test test/uat-runbooks.test.js`, repo checks, and AI-slop gates.
