---
status: complete
quick_id: 260705-303
slug: add-an-android-apk-package-artifact-with
date: 2026-07-05
---

# Quick Task 260705-303: Add an Android APK package artifact with truthful build proof

## Summary

- Added `npm run build:android:apk` to build the generated Android WebView native source with Gradle `assembleDebug`.
- Packages `meshdrop-android-apk-<version>.tar.gz` with `apk/meshdrop-android-debug.apk`, APK metadata, build proof JSON,
  app assets, and the Android native source used to build it.
- Marks Android APK manifests as `nativeShellBuilt: true` and `nativeShellSourceBuilt: true`, but keeps `webrtc`,
  `nostr`, and `bluetooth` disabled because no native mobile transfer/device UAT was run.
- Added `npm run test:android-apk`, CI smoke coverage, release artifact creation, release readback, and UAT ledger docs.

## Verification

- Generated current native source and proved it can build locally with:
  `ANDROID_HOME=$HOME/Android/Sdk ANDROID_SDK_ROOT=$HOME/Android/Sdk JAVA_HOME=/usr/lib/jvm/java-17-openjdk gradle --no-daemon --stacktrace assembleDebug`.
- `npm run test:android-apk` passed and emitted
  `Proof android-apk-build: built meshdrop-android-apk-0.0.0-smoke.tar.gz with 12345032 byte debug APK`.
- `node --test test/mobile-package.test.js test/ci-workflow.test.js test/release-workflow.test.js test/uat-runbooks.test.js`
  passed: 15/15.
- Initial `npm test` failed in the fresh worktree because dependencies were not installed (`nostr-tools`, `ws` missing).
- `npm ci` passed: 87 packages, 0 vulnerabilities.
- `npm test` passed: 188/188.
- `ruby -e 'require "yaml"; ...'` parsed the changed workflow files.
- `go run github.com/rhysd/actionlint/cmd/actionlint@latest .github/workflows/docker-image.yml .github/workflows/release.yml .github/workflows/release-verify.yml` passed.
- `git diff --check` passed.
- `npx --yes aislop scan --changes .` passed clean.
- `npx --yes aislop scan .` ran and failed on the existing repo-wide baseline: 461 `no-undef` browser global errors,
  3 existing `innerHTML` security findings in `public/scripts/ui.js`, plus existing size/duplicate/console/comment warnings.

## Remaining Gaps

- Android device or emulator install UAT is not proven.
- Native Android WebRTC transfer UAT is not proven.
- Mobile file picker/share sheet integration is not proven.
- Bluetooth transport negotiation is not implemented or proven.
- Signed Android release APK/AAB proof is not complete.
