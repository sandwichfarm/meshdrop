---
status: complete
quick_id: 260706-v15
slug: release-v015-readback
date: 2026-07-06
---

# Quick Task 260706-v15: Release v0.1.5 Readback

## Goal

Cut and verify alpha release `v0.1.5` after PRs #103-#107 landed.

## Scope

1. Require green master CI before tagging.
2. Push annotated tag `v0.1.5` from current `origin/master`.
3. Watch release workflow through GitHub release assets, GHCR target image publishing, and release readback.
4. Record authenticated release proof and keep anonymous GHCR visibility open.

## Validation

- Master CI run `28759999950` passed before tagging.
- Release run `28760231569` created expected assets.
- GHCR target image jobs passed for `standalone`, `start9`, and `umbrel`.
- Release readback verified assets, authenticated multi-arch manifests, pulled metadata, and Docker smoke.
- Release readback failed only at anonymous GHCR readback with `unauthorized`.
