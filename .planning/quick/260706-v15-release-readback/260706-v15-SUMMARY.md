---
status: complete
quick_id: 260706-v15
slug: release-v015-readback
date: 2026-07-06
completed_at: 2026-07-06
---

# Quick Task 260706-v15: Release v0.1.5 Readback

## Summary

- Cut and pushed alpha tag `v0.1.5` from green `master` commit `5876d8efea176afe55778880283602c96285551a`.
- Release run `28760231569` created GitHub release assets, including the latest Android physical-UAT-era artifact set.
- GHCR target image jobs passed for `standalone`, `start9`, and `umbrel`.
- Release readback passed GitHub release asset verification, authenticated GHCR multi-arch manifests, pulled target
  metadata, and Docker smoke against `ghcr.io/sandwichfarm/meshdrop:v0.1.5-standalone`.
- Release readback failed only at the strict anonymous GHCR manifest gate with `unauthorized`.

## Verification

- `gh run watch 28759999950 --repo sandwichfarm/meshdrop --exit-status` passed master CI before tagging.
- `git tag -a v0.1.5 5876d8efea176afe55778880283602c96285551a -m "MeshDrop v0.1.5"` and `git push origin v0.1.5` pushed the release tag.
- `gh run watch 28760231569 --repo sandwichfarm/meshdrop --exit-status` reached the expected failure at anonymous GHCR.
- `gh release view v0.1.5 --repo sandwichfarm/meshdrop --json ...` confirmed the live prerelease and asset list.
- Release readback job `85274751545` showed Docker smoke proof and `Get "https://ghcr.io/v2/sandwichfarm/meshdrop/manifests/v0.1.5-standalone": unauthorized`.

## Remaining Gaps

- Anonymous GHCR manifest readback remains blocked until `ghcr.io/sandwichfarm/meshdrop` is public, or the project
  decides authenticated-only GHCR pulls are acceptable.
- Deployed StartOS/Umbrel node UAT remains open.
- Signed/device-installable iOS package proof and iOS device transfer UAT remain open.
