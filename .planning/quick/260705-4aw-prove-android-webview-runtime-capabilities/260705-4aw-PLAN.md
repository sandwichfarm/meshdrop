---
status: in_progress
quick_id: 260705-4aw
slug: prove-android-webview-runtime-capabilities
date: 2026-07-05
---

# Quick Task 260705-4aw: Prove Android WebView runtime capabilities

## Goal

Add direct Android WebView runtime capability evidence for the debug APK without claiming native Android file transfer.

## Plan

1. Enable WebView DevTools inspection only when the generated Android app is debuggable.
   - File: `scripts/mobile-native-source.mjs`.
   - Keep this restricted to debug/UAT packages.

2. Add an Android WebView capability smoke.
   - File: `scripts/android-webview-capability-smoke.mjs`.
   - Build and install the debug APK, launch `farm.sandwich.meshdrop/.MainActivity`, attach to the WebView DevTools
     socket, and evaluate runtime primitives from inside the app WebView.

3. Wire the smoke into docs and tests.
   - Files: `package.json`, `docs/uat/mobile.md`, `docs/uat/target-status.md`, `test/uat-runbooks.test.js`,
     `test/mobile-package.test.js`.
   - Record capability proof separately from transfer proof.

4. Verify and ship.
   - Run the capability smoke against `Medium_Phone_API_36.1`.
   - Run focused tests, repo gates, and AI-slop gates.
   - Commit, push, open a PR, merge, and watch master CI.
