---
status: complete
quick_id: 260705-v13
slug: release-v013-readback
---

# Quick Task 260705-v13: Release v0.1.3 Readback

## Goal

Cut and verify alpha release `v0.1.3` after Desktop Chromium and mobile native-source artifacts landed.

## Scope

1. Push tag `v0.1.3` from green `master`.
2. Watch the release workflow through GitHub release artifact creation, GHCR target image publishing, and release readback.
3. Record current evidence without claiming anonymous GHCR visibility.

## Validation

- Master CI run `28723543649` passed before tagging.
- Release run `28723597015` created all expected assets.
- GHCR target image jobs passed for `standalone`, `start9`, and `umbrel`.
- Release readback verified assets, authenticated multi-arch manifests, pulled metadata, and Docker smoke.
- Release readback failed only at anonymous GHCR readback with `unauthorized`.
- `node --test test/uat-runbooks.test.js`
- `git diff --check`
- `npx --yes aislop scan --changes .`
