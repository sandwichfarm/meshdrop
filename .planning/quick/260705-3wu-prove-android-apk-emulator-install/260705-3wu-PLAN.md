---
status: in_progress
quick_id: 260705-3wu
slug: prove-android-apk-emulator-install
date: 2026-07-05
---

# Quick Task 260705-3wu: Prove Android APK emulator install

## Goal

Move Android APK proof from build-only to emulator install and launch proof without claiming native mobile transfer,
file picker/share sheet, Bluetooth, physical-device, or release-signing support.

## Plan

1. Add an Android APK install smoke.
   - File: `scripts/android-apk-install-smoke.mjs`.
   - Build the debug APK artifact, install it on an attached device or explicitly named local AVD, resolve
     `farm.sandwich.meshdrop/.MainActivity`, launch it, and confirm the activity is top.

2. Wire the smoke into project commands and UAT docs.
   - Files: `package.json`, `docs/uat/mobile.md`, `docs/uat/target-status.md`, `test/uat-runbooks.test.js`.
   - Keep CI cost controlled by making the emulator smoke an explicit command, not a default PR job.

3. Verify and ship.
   - Run the emulator smoke locally against `Medium_Phone_API_36.1`.
   - Run focused tests, repo gates, and AI-slop gates.
   - Commit, push, open a PR, merge, and watch master CI.
