# Quick Task 260706-s84: Fix client-side relay configuration defaults/editability and Docker admin pubkey

Date: 2026-07-06
Status: in-progress

## Goal

Make the Relays settings panel show real client-side defaults for bootstrap, WebRTC announcement, inbox, and outbox relays so users can edit them locally. Configure the default Docker Compose shared-instance admin to the requested hex pubkey so the signed FIPS settings UI can manage peers.

## Tasks

1. Add regression coverage that the browser relay preference globals used by the settings dialog exist and normalize to non-empty client defaults.
2. Add regression coverage that `docker-compose.yml` configures `MESHDROP_ADMIN_PUBKEY` to the requested admin hex key.
3. Patch the smallest surfaces: browser relay exports and compose env.
4. Verify focused tests, broad repo tests, diff checks, runtime-relevant smoke where feasible, and AI-slop gates.

## Acceptance

- Relay settings textareas hydrate from client defaults instead of staying blank.
- Saved relay settings still publish NIP-65 read/write tags.
- Docker Compose enables server admin settings for pubkey `e771af0b05c8e95fcdf6feb3500544d2fb1ccd384788e9f490bb3ee28e`.
