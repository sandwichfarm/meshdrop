---
phase: 09-spa-route-honesty
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - public/scripts/runtime-capabilities.js
  - public/scripts/network.js
  - public/scripts/ui.js
  - scripts/build-spa-artifact.mjs
  - scripts/spa-artifact-smoke.mjs
  - test/spa-runtime-config.test.js
  - test/peer-availability-protocol.test.js
  - test/rtc-peer-signaling.test.js
  - test/route-attempts-ui.test.js
  - docs/adr/0004-spa-route-honesty.md
autonomous: true
requirements:
  - SPA-01
  - SPA-02
  - SPA-03
  - SPA-04
  - SPA-05
  - SPA-06
  - SPA-07
user_setup: []
must_haves:
  truths:
    - Backend-free SPA artifacts cannot claim backend-only FIPS/Pollen/native byte transfer from badges, descriptors, or build metadata.
    - Pure-client browser transfers remain usable in SPA artifacts.
    - Peer-advertised private routes may be visible as unavailable/instance-dependent, but never as complete without route proof.
  key_links:
    - Static target manifest -> runtime capability negotiation.
    - Runtime capability negotiation -> PeerAvailabilityProtocol route filtering.
    - Runtime capability negotiation -> PeersManager route selection blocking.
    - SPA artifact smoke -> proof that pure-client transfers still work and backend-only controls/options stay unavailable.
  artifacts:
    - Red/green tests for static capability honesty and route choice filtering.
    - SPA artifact smoke proof after backend-only claims are blocked.
    - ADR documenting the static/browser boundary.
---

<objective>
Implement the SPA route honesty slice.

Purpose: make no-backend/static artifacts fail closed for backend-only transports while preserving browser-available routes.
Output: tested runtime capability and route UI behavior, SPA artifact smoke proof, and ADR.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Lock static runtime capability behavior</name>
  <requirements>SPA-01, SPA-02, SPA-03</requirements>
  <files>test/spa-runtime-config.test.js, test/spa-artifact.test.js, public/scripts/runtime-capabilities.js, scripts/build-spa-artifact.mjs</files>
  <action>Add failing tests showing static SPA config and target manifests do not advertise backend-only FIPS/Pollen/native transfer support from manifest booleans, build metadata, discovery descriptors, or badges alone. Implement fail-closed static capability negotiation and preserve unavailable reasons.</action>
  <verify>node --test test/spa-runtime-config.test.js test/spa-artifact.test.js</verify>
  <done>Static config tests prove pure-client routes remain available and backend-only routes fail closed.</done>
</task>

<task type="auto">
  <name>Task 2: Lock route choice and attempt honesty</name>
  <requirements>SPA-04, SPA-05, SPA-07</requirements>
  <files>test/peer-availability-protocol.test.js, test/rtc-peer-signaling.test.js, test/route-attempts-ui.test.js, public/scripts/ui.js, public/scripts/network.js</files>
  <action>Add route availability and signaling tests for backend-free config: peer-advertised FIPS/Pollen room IDs and routeCapabilities may render as unavailable attempts, but PeerAvailabilityProtocol.optionsFor cannot return selectable FIPS/Pollen direct options and PeersManager cannot select those routes unless runtime capabilities expose a real route primitive. Implement config-to-UI and config-to-network filtering.</action>
  <verify>node --test test/peer-availability-protocol.test.js test/rtc-peer-signaling.test.js test/route-attempts-ui.test.js</verify>
  <done>Route choice tests prove backend-only SPA routes are not selectable as file transports unless real primitives exist.</done>
</task>

<task type="auto">
  <name>Task 3: Prove SPA artifact transfer still works</name>
  <requirements>SPA-06</requirements>
  <files>scripts/spa-artifact-smoke.mjs, docs/adr/0004-spa-route-honesty.md</files>
  <action>Update SPA artifact smoke to assert pure-client Nostr WebRTC transfer and browser UI state: Instance/FIPS/Pollen controls hidden or unavailable, selectable route options exclude backend-only routes, and route-attempt UI does not show connected/transferring/complete for backend-only routes without proof. Document static/browser route boundary in ADR.</action>
  <verify>npm run test:spa-artifact</verify>
  <done>SPA artifact browser proof still transfers a file over pure-client Nostr WebRTC with backend-only route claims blocked.</done>
</task>

</tasks>

<verification>
- [x] node --test test/spa-runtime-config.test.js test/spa-artifact.test.js test/peer-availability-protocol.test.js test/rtc-peer-signaling.test.js test/route-attempts-ui.test.js
- [x] npm run test:spa-artifact
- [x] npm test
- [x] git diff --check
- [x] npx --yes aislop scan --changes .
</verification>

<success_criteria>
- Every v0.5.0 requirement in `.planning/REQUIREMENTS.md` maps to Phase 9.
- Static SPA artifacts report route capability honestly.
- Route choice UI cannot offer backend-only FIPS/Pollen/native routes as live file transports in backend-free targets.
- SPA artifact browser transfer proof still passes.
</success_criteria>
