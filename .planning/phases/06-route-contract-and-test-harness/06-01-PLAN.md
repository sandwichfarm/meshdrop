---
phase: 06-route-contract-and-test-harness
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - public/scripts/route-contract.js
  - public/scripts/main.js
  - public/service-worker.js
  - test/route-contract.test.js
  - docs/adr/0001-route-adapter-contract.md
autonomous: true
requirements:
  - ROUTE-01
  - ROUTE-02
  - ROUTE-03
  - ADAPT-01
  - ADAPT-02
  - ADAPT-03
  - SCORE-01
  - SCORE-02
  - PROOF-01
  - ADR-01
  - TEST-01
user_setup: []
must_haves:
  truths:
    - Route descriptors reject unsupported shapes, expiration, wrong sessions, and wrong owner bindings.
    - Legacy FIPS/Pollen room descriptors remain representable without changing live route selection.
    - Candidate scoring is deterministic and returns machine-readable reasons.
    - Route proof validation rejects hash mismatch and fallback.
  artifacts:
    - public/scripts/route-contract.js
    - test/route-contract.test.js
    - docs/adr/0001-route-adapter-contract.md
  key_links:
    - public/scripts/main.js loads the browser-safe route contract before route controllers.
    - public/service-worker.js caches the route contract asset.
---

<objective>
Introduce the route contract and behavior-first test harness for route descriptors, adapter availability, candidate scoring, and route proof.

Purpose: Later FIPS, Pollen, instance relay, native, and future transport slices need one shared contract before they can claim byte-transfer support.
Output: Shared browser-safe contract helper, tests, service-worker cache entry, and ADR.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Pin descriptor behavior</name>
  <files>test/route-contract.test.js, public/scripts/route-contract.js</files>
  <action>Write failing tests for roadmap-shaped descriptors, legacy room descriptors, invalid/expired/bad-binding rejection, then implement the smallest contract helper to pass.</action>
  <verify>node --test test/route-contract.test.js</verify>
  <done>Descriptor and legacy-room tests pass through public `MeshDropRouteContract`.</done>
</task>

<task type="auto">
  <name>Task 2: Pin adapter, scoring, and proof behavior</name>
  <files>test/route-contract.test.js, public/scripts/route-contract.js</files>
  <action>Add red/green tests for adapter method/status validation, deterministic candidate ranking, and route proof rejection of hash mismatch/fallback.</action>
  <verify>node --test test/route-contract.test.js</verify>
  <done>Adapter, scoring, and proof tests pass without touching live route selection.</done>
</task>

<task type="auto">
  <name>Task 3: Expose and document contract</name>
  <files>public/scripts/main.js, public/service-worker.js, docs/adr/0001-route-adapter-contract.md</files>
  <action>Load the route contract as a browser deferred script, cache it for PWA assets, and record the control-plane/data-plane ADR.</action>
  <verify>npm run build:service-worker; node --test test/route-contract.test.js test/nostr-mesh-protocol.test.js test/signaling-room-priority.test.js test/pollen-transfer-protocol.test.js</verify>
  <done>Contract is browser-visible, cached, documented, and current room-based route tests remain green.</done>
</task>

</tasks>

<verification>
- [x] node --test test/route-contract.test.js
- [x] node --test test/route-contract.test.js test/nostr-mesh-protocol.test.js test/signaling-room-priority.test.js test/pollen-transfer-protocol.test.js
- [x] npm ci
- [x] npm test
- [x] npm run test:e2e
- [x] git diff --check
- [x] npx --yes aislop scan --changes .
</verification>

<success_criteria>
- All Phase 6 requirements mapped in `.planning/REQUIREMENTS.md`.
- Contract helper is pure and does not rewrite live route selection.
- Existing room-based Nostr/FIPS/Pollen tests stay green.
- Local full test suite passes.
</success_criteria>
