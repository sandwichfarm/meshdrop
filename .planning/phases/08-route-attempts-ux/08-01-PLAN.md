---
phase: 08-route-attempts-ux
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - public/scripts/ui.js
  - public/styles/styles-deferred.css
  - test/route-attempts-ui.test.js
  - test/peer-availability-protocol.test.js
  - scripts/e2e-smoke.mjs
  - docs/adr/0003-route-attempts-ux.md
autonomous: true
requirements:
  - UX-01
  - UX-02
  - UX-03
  - UX-04
  - UX-05
  - UX-06
  - UX-07
user_setup: []
must_haves:
  truths:
    - Route UI explains attempts, reasons, privacy labels, and proof-backed completion.
    - Discovery, descriptor, badge, or route-candidate state alone never renders as transfer success.
    - Unsupported backend-only routes are hidden or disabled unless current runtime can transfer bytes through them.
    - Copy is user-facing and avoids route descriptor/protocol internals.
  key_links:
    - `peer-route-status` events become UI route-attempt summaries.
    - Route choice options expose structured attempt state for direct, Nostr, instance, FIPS, Pollen, and object-store candidates.
    - `route-proof` summaries require matching bytes, hash match, and no fallback before completion.
    - Tests cover state/reason/privacy copy and false-success regression.
  artifacts:
    - Focused red/green tests for route-attempt presenter behavior.
    - Peer/transfer UI route-attempt detail surface.
    - Browser smoke evidence that existing transfers still work with the new route-attempt UI.
    - ADR for route-attempt UI boundary.
---

<objective>
Implement the route-attempt UX slice.

Purpose: make MeshDrop route choice and transfer state understandable without implying unsupported transports or exposing protocol internals.
Output: tested route-attempt presenter, peer/transfer UI detail surface, proof-backed completion mapping, and ADR.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Pin route-attempt presentation contract</name>
  <requirements>UX-01, UX-02, UX-03, UX-04, UX-05, UX-07</requirements>
  <files>test/route-attempts-ui.test.js, test/peer-availability-protocol.test.js, public/scripts/ui.js</files>
  <action>Write failing tests for route-attempt state labels, user-facing failure reasons, privacy labels, proof-backed completion, and stale badge/discovery states not counting as successful transfer.</action>
  <verify>node --test test/route-attempts-ui.test.js test/peer-availability-protocol.test.js</verify>
  <done>Tests prove route status and route proof map to explicit UI summaries before implementation is accepted.</done>
</task>

<task type="auto">
  <name>Task 2: Render route attempts in peer and transfer UI</name>
  <requirements>UX-01, UX-02, UX-03, UX-04</requirements>
  <files>public/scripts/ui.js, public/styles/styles-deferred.css, test/route-attempts-ui.test.js</files>
  <action>Add a small presenter on existing PeerAvailability/PeerRouteStatus protocols and render a compact route-attempt detail row under peer status plus route-attempt metadata in the transfer choice list.</action>
  <verify>node --test test/route-attempts-ui.test.js test/peer-availability-protocol.test.js</verify>
  <done>Peer UI exposes candidate/requested/connecting/transferring/complete/unavailable/failed/expired/blocked-fallback states with reason/privacy labels.</done>
</task>

<task type="auto">
  <name>Task 3: Keep unsupported and proof semantics honest</name>
  <requirements>UX-05, UX-06, UX-07</requirements>
  <files>public/scripts/ui.js, scripts/e2e-smoke.mjs, docs/adr/0003-route-attempts-ux.md</files>
  <action>Ensure route proof completion requires matching bytes, hash match, and no fallback; backend-only routes render disabled/hidden when unavailable; document the UI boundary and add e2e evidence for existing transfer behavior.</action>
  <verify>npm run test:e2e</verify>
  <done>Existing transfer proof still passes and the UI contract cannot mark route success from discovery, descriptor, or badge-only state.</done>
</task>

</tasks>

<verification>
- [x] node --test test/route-attempts-ui.test.js test/peer-availability-protocol.test.js test/route-contract.test.js
- [x] npm test
- [x] npm run test:e2e
- [x] git diff --check
- [x] npx --yes aislop scan --changes .
</verification>

<success_criteria>
- Every v0.4.0 requirement in `.planning/REQUIREMENTS.md` maps to Phase 8.
- Route attempt UI names route candidates, attempt state, user-facing reason, and privacy label.
- Completed route detail comes from proof fields and requires byte/hash/fallback evidence.
- Existing local/Nostr/Pollen browser transfer smokes still pass.
</success_criteria>
