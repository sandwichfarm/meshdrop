---
phase: 10-android-native-route-adapter
plan: 01
type: tdd
wave: 1
depends_on:
  - 09-spa-route-honesty
files_modified:
  - public/scripts/android-native-routes.js
  - public/scripts/main.js
  - scripts/android-fips-pollen-smoke.mjs
  - test/android-native-route-adapter.test.js
  - test/mobile-package.test.js
autonomous: true
requirements:
  - ANDROID-NATIVE-01
  - ANDROID-NATIVE-02
  - ANDROID-NATIVE-03
  - ANDROID-NATIVE-04
  - ANDROID-NATIVE-05
user_setup: []
must_haves:
  truths:
    - Android native route support exists only when the WebView loopback backend is alive.
    - Native Pollen proof must exercise adapter send and receive, not only fetch status.
    - FIPS status can be exposed, but FIPS byte transfer remains unproven until a FIPS data-plane proof exists.
  key_links:
    - Android bootstrap backend URL -> adapter status.
    - Adapter send/receive -> native Pollen upload/download endpoint.
    - Adapter proof -> generic route proof validator.
  artifacts:
    - Red/green unit tests for adapter contract and proof.
    - Mobile package readback proving adapter script ships.
    - Installed APK smoke proving native Pollen route proof.
---

<objective>
Implement the Android native route adapter slice.

Purpose: expose native Android FIPS/Pollen backend capabilities through the route adapter contract and prove native Pollen bytes/hash/fallback fields from the installed APK.
Output: adapter script, package inclusion, installed APK route proof, and updated GSD summary.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Lock adapter contract behavior</name>
  <requirements>ANDROID-NATIVE-01, ANDROID-NATIVE-02, ANDROID-NATIVE-03</requirements>
  <files>test/android-native-route-adapter.test.js, public/scripts/android-native-routes.js, public/scripts/main.js</files>
  <action>Add failing tests for unavailable backend, available Android backend, validated adapter surface, Pollen descriptor/send/receive/proof, and FIPS status without transfer overclaim. Implement the adapter.</action>
  <verify>node --test test/android-native-route-adapter.test.js</verify>
  <done>Adapter passes `MeshDropRouteContract.validateAdapter` only with live backend metadata and validates Pollen proof.</done>
</task>

<task type="auto">
  <name>Task 2: Package adapter into Android artifacts</name>
  <requirements>ANDROID-NATIVE-05</requirements>
  <files>test/mobile-package.test.js, public/scripts/main.js</files>
  <action>Prove Android native source/APK artifacts include the adapter script without changing source-only manifest transport honesty.</action>
  <verify>node --test test/mobile-package.test.js</verify>
  <done>Mobile package readback sees `app/scripts/android-native-routes.js` and Android wrapper still injects backend URL only at runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Prove installed APK route proof</name>
  <requirements>ANDROID-NATIVE-04</requirements>
  <files>scripts/android-fips-pollen-smoke.mjs</files>
  <action>Update the installed APK FIPS/Pollen smoke to call the Android native route adapter, send/receive a proof file, validate byte/hash/fallback fields, and print the route proof fields.</action>
  <verify>npm run test:android-fips-pollen</verify>
  <done>Installed APK emits validated Android native Pollen route proof.</done>
</task>

</tasks>

<verification>
- [x] node --test test/android-native-route-adapter.test.js
- [x] node --test test/mobile-package.test.js
- [x] npm run test:android-fips-pollen
- [x] npm test
- [x] git diff --check
- [x] npx --yes aislop scan --changes .
</verification>

<success_criteria>
- Android native adapter is contract-valid only with live native backend.
- Native Pollen send/receive proof validates with `MeshDropRouteContract.validateRouteProof`.
- FIPS native status is reported but not claimed as byte-transfer proof.
- Installed APK smoke prints sender runtime, recipient runtime, route type, data-plane primitive, WebRTC false, instance relay false, bytes, hash match, and fallback false.
</success_criteria>
