---
phase: 07-pollen-instance-relay-proof
plan: 01
type: tdd
wave: 1
depends_on:
  - 06-route-contract-and-test-harness
files_modified:
  - public/scripts/pollen-transfer.js
  - public/scripts/network.js
  - test/pollen-instance-relay.test.js
  - scripts/e2e-smoke.mjs
  - docs/adr/0002-pollen-instance-relay.md
autonomous: true
requirements:
  - PIR-01
  - PIR-02
  - PIR-03
  - PIR-04
  - PIR-05
  - PIR-06
  - PIR-07
  - PIR-08
  - PIR-09
user_setup: []
must_haves:
  truths:
    - Encrypted chunks cross two MeshDrop instances over Pollen.
    - Route proof says Pollen carried the selected data plane.
    - WebRTC and local instance fallback do not satisfy this phase.
    - Missing runtime, bad descriptor/session/owner, hash mismatch, and fallback attempts fail closed.
  key_links:
    - Sender browser encrypts deterministic bytes before local-instance upload.
    - Sender instance publishes only ciphertext through Pollen-backed relay primitive.
    - Recipient instance retrieves ciphertext by descriptor-bound route/session/owner.
    - Recipient browser decrypts locally and verifies original file hash.
    - Proof binds route type, primitive, runtimes, byte counts, hash, and fallback status.
  artifacts:
    - Focused red/green tests for Pollen instance relay.
    - Runtime proof that exercises two instances, isolated stores, forced Pollen route, and disabled WebRTC/local fallback.
    - ADR for Pollen instance relay boundaries.
---

<objective>
Implement the first instance-mediated backend byte-transfer route: Pollen instance relay.

Purpose: prove the route contract can move real encrypted file bytes over one selected backend route before adding FIPS or more networks.
Output: tested relay path, route proof, fail-closed behavior, and ADR.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Pin relay proof contract</name>
  <files>test/pollen-instance-relay.test.js, public/scripts/route-contract.js</files>
  <action>Write failing tests for successful Pollen instance-relay route proof and fail-closed proof rejection for missing runtime, expired descriptor, wrong session, wrong owner, hash mismatch, and fallback attempts.</action>
  <verify>node --test test/pollen-instance-relay.test.js</verify>
  <done>Route proof contract distinguishes real Pollen instance relay from discovery or fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Implement browser-to-instance relay byte path</name>
  <files>server.js, public/scripts/pollen-transfer.js, public/scripts/file-transfer.js, test/pollen-instance-relay.test.js</files>
  <action>Add the smallest route-wired encrypted-chunk path where sender browser encrypts bytes, sender local instance relays ciphertext through Pollen-backed storage/service behavior, recipient local instance retrieves ciphertext, and recipient browser decrypts/verifies hash locally.</action>
  <verify>node --test test/pollen-instance-relay.test.js test/pollen-transfer-protocol.test.js</verify>
  <done>Two-instance focused test proves ciphertext-only instance/Pollen storage, bytes sent, bytes received, and hash match.</done>
</task>

<task type="auto">
  <name>Task 3: Add runtime proof and ADR</name>
  <files>scripts/e2e-smoke.mjs, docs/adr/0002-pollen-instance-relay.md</files>
  <action>Extend runtime smoke or add focused runtime proof with two MeshDrop processes/instances, isolated stores, forced Pollen route, disabled WebRTC/local fallback, and session-correlated Pollen upload/download evidence; document instance/metadata/plaintext boundaries.</action>
  <verify>npm run test:e2e and npm run test:docker if server/Docker-visible behavior changed</verify>
  <done>Runtime proof logs Pollen instance relay with WebRTC/local fallback disabled for the selected route and correlates transfer/session ID through both instances plus Pollen evidence.</done>
</task>

</tasks>

<verification>
- [x] node --test test/pollen-instance-relay.test.js
- [x] node --test test/pollen-instance-relay.test.js test/pollen-transfer-protocol.test.js test/route-contract.test.js test/blossom-key-delivery.test.js
- [x] npm test
- [x] npm run test:e2e
- [x] npm run test:docker not required: no Docker/compose/server container behavior changed
- [x] git diff --check
- [x] npx --yes aislop scan --changes .
</verification>

<success_criteria>
- Every v0.3.0 requirement in `.planning/REQUIREMENTS.md` maps to Phase 7.
- Pollen instance relay transfer proof names bytes, route type, runtimes, primitive, hash match, and fallback status.
- Current direct WebRTC, Pollen object transfer, and Nostr signaling tests remain green.
</success_criteria>
